version: '3.8'

services:
  karukera-telemetry:
    build: .
    container_name: karukera_hub
    restart: always
    # Crucial for Bleak BLE to tap directly into the host OS bluetooth hardware adapter layers
    network_mode: "host"
    volumes:
      - /var/run/dbus:/var/run/dbus
      - .:/app